'use strict'

var React = require('react')

var Title = React.createClass({
    render: function () {
        return <h1>React usando JSX na Aplicação</h1>
    }
})

export default Title // esta importando o Title, utilizando codigo do ES2015